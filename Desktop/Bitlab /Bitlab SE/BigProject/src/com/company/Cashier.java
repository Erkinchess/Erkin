package com.company;

import javax.swing.*;
import java.awt.*;

public class Cashier extends JFrame {
    public Cashier(Frame parent) {
        setSize(500, 280);
        setBackground(Color.LIGHT_GRAY);
        setLayout(null);
    }
}